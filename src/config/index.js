/* eslint-disable react-hooks/rules-of-hooks */
import {useTheme} from 'cant-ui/theme';
const {colors} = useTheme();

const Config = {
    colors: {
        main: colors.info
    }
}

export default  Config;
